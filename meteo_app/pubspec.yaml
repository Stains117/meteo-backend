name: meteo_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^0.13.5
  geolocator: ^13.0.2
  permission_handler: ^11.0.1  # Aggiunto per gestire i permessi GPS
  flutter_map: ^4.0.0
  latlong2: ^0.8.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

map_dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  geolocator: ^10.1.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
